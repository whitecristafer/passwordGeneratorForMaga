import java.util.Scanner;

public class logic {
    public static void start() {
        Scanner sc = new Scanner(System.in);

        String fullName = sc.nextLine();

        System.out.println(password.create(fullName.toLowerCase()));
        sc.close();
    }
}
